{
  "name": "elemine-server",
  "version": "1.0.0",
  "author": "codesprut",
  "license": "MIN",
  "description": "Elemine launcher server",
  "main": "run.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node start.js",
    "start:dev": "cross-env DEBUG=socket.io* node start.js"
  },
  "dependencies": {
    "socket.io": "^3.1.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
